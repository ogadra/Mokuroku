use flake

# workerd (BoringSSL) does not auto-discover the system CA bundle and relies on
# SSL_CERT_FILE, so point it at one for local `pnpm dev` outbound HTTPS (connpass sync).
export SSL_CERT_FILE="${SSL_CERT_FILE:-/etc/ssl/certs/ca-certificates.crt}"
